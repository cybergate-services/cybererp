<odoo>

    <record model="res.groups" id="group_asterisk_base_admin">
        <field name="name">Asterisk Administrator</field>
        <field name="category_id" ref="module_asterisk_base_category"/>        
        <field name="implied_ids" eval="[(4, ref('group_asterisk_base_user'))]"/>
        <field name="users" eval="[(4, ref('base.user_admin'))]"/>
    </record>

</odoo>